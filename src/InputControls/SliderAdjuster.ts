import { css } from '@emotion/css'
import { createElement as $, FC } from 'react';

export interface SliderAdjusterProps {
  // props...
}

export const $SliderAdjuster: FC<SliderAdjusterProps> = ({}) => {
  return $('div', {
    className: css({
      // code...
    })
  });
};
